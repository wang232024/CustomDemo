<?xml version="1.0" encoding="utf-8"?>
<paths>
    <external-path          name="external_storage_root"    path="." />     <!--Environment.getExternalStorageDirectory()-->

    <files-path             name="files-path"               path="." />     <!--content.getFileDir()-->

    <cache-path             name="cache-path"               path="." />     <!--content.getCacheDir()-->

    <!--/storage/emulated/0/Android/data/...-->
    <external-files-path    name="external_file_path"       path="." />     <!--ContextCompat.getExternalFilesDirs()-->

    <!--代表app 外部存储区域根目录下的文件 Context.getExternalCacheDir目录下的目录-->
    <external-cache-path    name="external_cache_path"      path="." />     <!--ContextCompat.getExternalCacheDirs()-->

    <!--配置root-path。这样子可以读取到sd卡和一些应用分身的目录-->
    <root-path              name="root-path"                path="" />      <!--File DEVICE_ROOT = new File("/")-->

</paths>